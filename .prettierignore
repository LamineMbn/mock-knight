dist/
node_modules/
pnpm-lock.yaml
mock-knight-logo-kit/
LICENSE

# The specs are hand-formatted and are the source of truth. Prettier column-aligns markdown
# tables, which rewrites most of these files for no benefit and buries real edits in churn.
*.md
design/*.md

# Generated by design/design-tokens.py — `pnpm tokens:css` writes it and the header says not to
# edit it by hand. Formatting it here would mean every regeneration reintroduced a diff.
packages/web/src/styles/tokens.css

# Generated by `pnpm schema` from the zod schema in packages/server/src/config.ts.
schema/mock-knight.schema.json
